# Auto detect text files and perform LF normalization
* text=auto

# Immutable raw API capture: preserve the fetched bytes; semantic JSON hashes
# separately protect its parsed evidence across platforms.
data/raw/external/kalshi_senate.json -text
